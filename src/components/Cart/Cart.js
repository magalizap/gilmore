//import React, { useContext } from 'react'
//import { AppContext } from '../../app/CartContext'

const Cart = () => {
  //const [cart, setCart] = useContext(AppContext)
  return (
    <div>

    </div>
  )
}

export default Cart

/**
 *       {cart.map((product, id) =>(
        <div key={id}>
          Nombre: {product.title} - Price: {product.price}
        </div>
      ))} 
 */